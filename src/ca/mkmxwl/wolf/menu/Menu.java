package ca.mkmxwl.wolf.menu;

import java.awt.*;

import ca.mkmxwl.wolf.*;

public class Menu {

	// all menus use the same controls for moving up and down and selecting options
	// all menus have similar variables: selected, options, etc.
	// similar rendering, have a base rendering then do the menu specific rendering after

	public void update(Game game, boolean up, boolean down, boolean enter) {
	}

	public void render(Graphics g) {
	}
}